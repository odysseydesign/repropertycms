# Block direct execution of PHP files in this directory and all subdirectories.
# Uploaded images/logos must never be interpreted as PHP.
<FilesMatch "\.(php[0-9]?|phtml|phar|shtml|cgi)$">
    Require all denied
</FilesMatch>
